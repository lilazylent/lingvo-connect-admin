# Owner Phase 05 review fix — 2026-09-15

Source: `docs/references/OWNER_PHASE05_REVIEW_TRANSCRIPT_2026-09-15.pdf`

This review is a binding acceptance supplement for Phase 05. It does not replace the Master Specification or the earlier owner transcript.

## Fix now before Phase 06

1. **Branding:** bring the `LC/` mark closer to the public-site treatment. Keep `АДМИН-ПАНЕЛЬ` narrower than the Lingvo Connect wordmark and right-align its final `Ь` under the final `T` in `Connect`.
2. **Applications row menu:** the three-dot menu must close on outside click and Escape; choosing an action must also close it.
3. **Applications detail contrast:** remove low-contrast blue legacy copy in Dark; use semantic primary/secondary/muted text and semantic surfaces.
4. **Applications header grid:** current status, status selector and relation actions must share one intentional control area rather than float at unrelated vertical positions.
5. **Decorative accent line:** the global burgundy line must no longer protrude sharply from card/page edges. It is a short, subtle gradient aligned inside the content grid.
6. **Comments/cards:** remove residual full-width/edge divider artifacts so rounded cards remain visually clipped and clean.
7. **Order work rows:** the `01` work index and service title must align as one balanced identity row.
8. **Order finances:** reduce tile height/padding and remove the oversized visual footprint while keeping total, debt, profit, margin and executor cost readable.
9. **Preliminary calculation:** keep the successful collapsed utility behavior from Phase 05.

## Deferred by the review

### Phase 06 — Files + Users + Settings
- white download buttons and white file separators in Dark;
- blue copy in Files;
- arrows/icons crossing card bounds;
- outside-click behavior for remaining row menus;
- Users menu overflow/viewport placement bug;
- Settings consolidation and interaction audit.

### Phase 07 — exhaustive Dark audit
- cross-route elimination of remaining white islands and low-contrast legacy colors.

### Phase 08 — Motion / EFX
- restrained hover/transition polish after layout and colors are stable.

### Phase 09 — final regression
- replay this review together with both earlier owner transcripts and the 26-point master spec.
